<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.otter.manager.biz.config.node.dal.dataobject.NodeDO">
    <resultMap id="nodeResult" type="node">
        <result property="id" column="ID" />
        <result property="name" column="NAME" />
        <result property="ip" column="IP" />
        <result property="port" column="PORT" />
        <result property="description" column="DESCRIPTION" />
        <result property="parameters" column="PARAMETERS" />
        <result property="gmtCreate" column="GMT_CREATE" />
        <result property="gmtModified" column="GMT_MODIFIED" />
    </resultMap>
    <!-- all node columns -->
    <sql id="allNodeColumns">ID,NAME,IP,PORT,DESCRIPTION,PARAMETERS,GMT_CREATE,GMT_MODIFIED
	</sql>
    <select id="findNodeById" resultMap="nodeResult" parameterType="long">
		select
		<include refid="allNodeColumns" />
		from NODE where ID = #{value}
	</select>
    <select id="getNodeCount" resultType="Integer">
		select count(*) from NODE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
				NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ip like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>
    </select>
    <select id="listNodes" resultMap="nodeResult">
		select
		<include refid="allNodeColumns" />
		from NODE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
				NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ip like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>

		ORDER BY ID DESC

		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
    <select id="listNodeByIds" resultMap="nodeResult">
		select
		<include refid="allNodeColumns" />
		from NODE where ID in
		<foreach open="(" close=")" collection="(_parameter instanceof java.util.Map ? (_parameter.containsKey(&quot;array&quot;) ? array : list) : _parameter)" item="item" separator=",">
			#{item}
		</foreach>
		ORDER BY ID DESC
	</select>
    <update id="updateNode" parameterType="node">
		update NODE
		set
		NAME=#{name},
		IP=#{ip},
		PORT=#{port},
		DESCRIPTION=#{description},
		PARAMETERS=#{parameters},
		GMT_MODIFIED=now()
		WHERE ID = #{id}
	</update>
    <select id="checkNodeUnique" parameterType="node" resultType="Integer">
		select count(*) from NODE
		where NODE.ID != #{id}
		and
		NODE.NAME = #{name}
		and NODE.IP = #{ip}
	</select>
    <insert id="insertNode" parameterType="node">
		insert into NODE
		(NAME,IP, PORT, DESCRIPTION, PARAMETERS,GMT_CREATE,
		GMT_MODIFIED)
		SELECT
		#{name},#{ip},#{port},#{description},#{parameters},now(),now()
		from dual
		WHERE not exists (select * from NODE
		where NODE.NAME = #{name} or
		( NODE.IP = #{ip} and
		NODE.PORT = #{port} )
		);
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			select
			last_insert_id()
		</selectKey>
    </insert>
    <delete id="deleteNodeById" parameterType="long">
        delete from NODE where ID = #{value}
    </delete>
</mapper>
