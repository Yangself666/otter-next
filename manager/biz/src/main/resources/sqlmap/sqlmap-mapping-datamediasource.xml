<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.otter.manager.biz.config.datamediasource.dal.dataobject.DataMediaSourceDO">
    <resultMap id="dataMediaSourceResult" type="dataMediaSource">
        <result property="id" column="ID" />
        <result property="name" column="NAME" />
        <result property="type" column="TYPE" />
        <result property="properties" column="PROPERTIES" />
        <result property="gmtCreate" column="GMT_CREATE" />
        <result property="gmtModified" column="GMT_MODIFIED" />
    </resultMap>
    <!-- all dataMediaSource columns -->
    <sql id="allDataMediaSourceColumns">ID,NAME,TYPE,PROPERTIES,GMT_CREATE,GMT_MODIFIED</sql>
    <select id="findDataMediaSourceById" resultMap="dataMediaSourceResult" parameterType="long">
		select
		<include refid="allDataMediaSourceColumns" />
		from DATA_MEDIA_SOURCE where ID = #{value}
	</select>
    <select id="getSourceCount" resultType="Integer">
		select count(*) from DATA_MEDIA_SOURCE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
			or TYPE like concat('%',replace(#{searchKey},'_','\_'),'%')
			or PROPERTIES like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>
    </select>
    <select id="listDataMediaSources" resultMap="dataMediaSourceResult">
		select
		<include refid="allDataMediaSourceColumns" />
		from DATA_MEDIA_SOURCE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
			or TYPE like concat('%',replace(#{searchKey},'_','\_'),'%')
			or PROPERTIES like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>

		ORDER BY ID DESC

		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
    <select id="listSourceByIds" resultMap="dataMediaSourceResult">
		select
		<include refid="allDataMediaSourceColumns" />
		from DATA_MEDIA_SOURCE where ID in
		<foreach open="(" close=")" collection="(_parameter instanceof java.util.Map ? (_parameter.containsKey(&quot;array&quot;) ? array : list) : _parameter)" item="item" separator=",">
			#{item}
		</foreach>
		ORDER BY ID DESC
	</select>
    <update id="updateDataMediaSource" parameterType="dataMediaSource">
		update DATA_MEDIA_SOURCE
		set
		NAME=#{name},
		TYPE=#{type},
		PROPERTIES=#{properties},
		GMT_MODIFIED=now()
		WHERE ID = #{id}
	</update>
    <select id="checkDataMediaSourceUnique" parameterType="dataMediaSource" resultType="Integer">
		select count(*) from DATA_MEDIA_SOURCE
		where DATA_MEDIA_SOURCE.ID != #{id}
		and DATA_MEDIA_SOURCE.NAME = #{name}
	</select>
    <insert id="insertDataMediaSource" parameterType="dataMediaSource">
		insert into DATA_MEDIA_SOURCE
		(NAME,TYPE,PROPERTIES,GMT_CREATE, GMT_MODIFIED)
		SELECT #{name},#{type},#{properties},now(),now()
		from dual
		WHERE not exists (select * from DATA_MEDIA_SOURCE
		where DATA_MEDIA_SOURCE.NAME = #{name} );
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			select
			last_insert_id()
		</selectKey>
    </insert>
    <delete id="deleteDataMediaSourceById" parameterType="long">
        delete from DATA_MEDIA_SOURCE where ID = #{value}
    </delete>
</mapper>
