<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.otter.manager.biz.config.datamatrix.dal.dataobject.DataMatrixDO">
    <resultMap id="dataMatrixResult" type="dataMatrix">
        <result property="id" column="ID" />
        <result property="groupKey" column="GROUP_KEY" />
        <result property="description" column="DESCRIPTION" />
        <result property="master" column="MASTER" />
        <result property="slave" column="SLAVE" />
        <result property="gmtCreate" column="GMT_CREATE" />
        <result property="gmtModified" column="GMT_MODIFIED" />
    </resultMap>
    <!-- all columns -->
    <sql id="allColumns">ID,GROUP_KEY,MASTER,SLAVE,DESCRIPTION,GMT_CREATE,GMT_MODIFIED
	</sql>
    <update id="updateDataMatrix" parameterType="dataMatrix">
		update DATA_MATRIX
		set
		GROUP_KEY=#{groupKey},
		MASTER=#{master},
		SLAVE=#{slave},
		DESCRIPTION=#{description},
		GMT_MODIFIED=now()
		WHERE ID = #{id}
	</update>
    <select id="checkDataMatrixUnique" parameterType="dataMatrix" resultType="Integer">
		select count(*) from DATA_MATRIX
		where ID != #{id}
		and
		GROUP_KEY = #{groupKey}
	</select>
    <insert id="insertDataMatrix" parameterType="dataMatrix">
		insert into DATA_MATRIX
		(GROUP_KEY,MASTER,SLAVE,DESCRIPTION,GMT_CREATE,GMT_MODIFIED)
		SELECT
		#{groupKey},#{master},#{slave},#{description},now(),now()
		from dual
		WHERE not exists (select * from DATA_MATRIX
		where DATA_MATRIX.GROUP_KEY = #{groupKey} );
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			select
			last_insert_id()
		</selectKey>
    </insert>
    <delete id="deleteDataMatrixById" parameterType="long">
        delete from DATA_MATRIX where ID = #{value}
    </delete>
    <select id="findDataMatrixByGroupKey" resultMap="dataMatrixResult" parameterType="string">
		select
		<include refid="allColumns" />
		from DATA_MATRIX
		where GROUP_KEY = #{groupKey}
	</select>
    <select id="listDataMatrixByIds" resultMap="dataMatrixResult">
		select
		<include refid="allColumns" />
		from DATA_MATRIX where ID in
		<foreach open="(" close=")" collection="(_parameter instanceof java.util.Map ? (_parameter.containsKey(&quot;array&quot;) ? array : list) : _parameter)" item="item" separator=",">
			#{item}
		</foreach>
		ORDER BY ID DESC
	</select>
    <select id="getDataMatrixCount" resultType="Integer">
		select count(*) from DATA_MATRIX
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
				GROUP_KEY like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
				or MASTER like concat('%',replace(#{searchKey},'_','\_'),'%')
				or SLAVE like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>
    </select>
    <select id="listDataMatrixs" resultMap="dataMatrixResult">
		select
		<include refid="allColumns" />
		from DATA_MATRIX
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
				GROUP_KEY like concat('%',replace(#{searchKey},'_','\_'),'%')
				or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
				or MASTER like concat('%',replace(#{searchKey},'_','\_'),'%')
				or SLAVE like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>
		ORDER BY ID DESC
		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
</mapper>
