<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.otter.manager.biz.user.dal.dataobject.UserDO">
    <resultMap id="userResult" type="user">
        <result property="id" column="ID" />
        <result property="name" column="USERNAME" />
        <result property="password" column="PASSWORD" />
        <result property="department" column="DEPARTMENT" />
        <result property="realName" column="REALNAME" />
        <result property="authorizeType" column="AUTHORIZETYPE" />
        <result property="gmtCreate" column="GMT_CREATE" />
        <result property="gmtModified" column="GMT_MODIFIED" />
    </resultMap>
    <!-- all user columns -->
    <sql id="allUserColumns">ID,USERNAME,PASSWORD,DEPARTMENT,REALNAME,AUTHORIZETYPE,GMT_CREATE,GMT_MODIFIED</sql>
    <!-- getUserById -->
    <select id="findUserById" resultMap="userResult" parameterType="long">
		select
		<include refid="allUserColumns" />
		from USER where ID = #{value}
	</select>
    <select id="getUserCount" resultType="Integer">
		select count(*) from USER
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			USERNAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
			or DEPARTMENT like concat('%',replace(#{searchKey},'_','\_'),'%')
			or REALNAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>
    </select>
    <!-- listUsers -->
    <select id="listUsers" resultMap="userResult">
		select
		<include refid="allUserColumns" />
		from USER
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			USERNAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%')
			or DEPARTMENT like concat('%',replace(#{searchKey},'_','\_'),'%')
			or REALNAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			</if>
        </trim>

		ORDER BY ID DESC

		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
    <update id="updateUser" parameterType="user">
		update USER
		set
		<trim>
            <if test="name != null and name != ''">
				USERNAME=#{name},
			</if>
            <if test="password != null and password != ''">
				PASSWORD=#{password},
			</if>
        </trim>
		DEPARTMENT=#{department},
		REALNAME=#{realName},
		AUTHORIZETYPE=#{authorizeType},
		GMT_MODIFIED=now()
		WHERE ID = #{id}
	</update>
    <select id="checkUserUnique" parameterType="user" resultType="Integer">
		select count(*) from USER
		where `USER`.ID != #{id}
		and `USER`.USERNAME = #{name}
	</select>
    <!--
      - ===============================================
      - 验证name、password
      - ===============================================
     -->
    <select id="getUserByNameAndPassword" resultMap="userResult" parameterType="user">
        select
                <include refid="allUserColumns" />
        from
                USER
        where
                USERNAME = #{name}
            and PASSWORD = #{password}
    </select>
    <insert id="insertUser" parameterType="user">
		insert into USER
		(USERNAME, PASSWORD,DEPARTMENT,REALNAME,AUTHORIZETYPE, GMT_CREATE, GMT_MODIFIED)
		SELECT #{name},#{password},#{department},#{realName},#{authorizeType},now(),now()
		from dual
		WHERE not exists (select * from USER
		where `USER`.USERNAME = #{name});
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			select last_insert_id()
		</selectKey>
    </insert>
    <delete id="deleteUserById" parameterType="long">
        delete from USER where ID = #{value}
    </delete>
    <select id="findUserByName" parameterType="string" resultMap="userResult">
        SELECT <include refid="allUserColumns" /> FROM USER WHERE USERNAME = #{value}
    </select>
</mapper>
