<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.otter.manager.biz.config.pipeline.dal.dataobject.PipelineDO">
    <resultMap id="pipelineResult" type="pipeline">
        <result property="id" column="ID" />
        <result property="name" column="NAME" />
        <result property="parameters" column="PARAMETERS" />
        <result property="description" column="DESCRIPTION" />
        <result property="channelId" column="CHANNEL_ID" />
        <result property="gmtCreate" column="GMT_CREATE" />
        <result property="gmtModified" column="GMT_MODIFIED" />
    </resultMap>
    <!-- all pipeline columns -->
    <sql id="allPipelineColumns">ID,NAME,PARAMETERS,DESCRIPTION,CHANNEL_ID,GMT_CREATE,GMT_MODIFIED
	</sql>
    <select id="findPipelineById" resultMap="pipelineResult" parameterType="long">
		select
		<include refid="allPipelineColumns" />
		from PIPELINE where ID = #{value}
	</select>
    <select id="getPipelineCount" resultType="Integer">
		select count(*) from PIPELINE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			(NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%'))
			</if>
            <if test="channelId != null">AND CHANNEL_ID = #{channelId}</if>
        </trim>
    </select>
    <select id="listPipelinesByChannelIds" resultMap="pipelineResult">
		select
		<include refid="allPipelineColumns" />
		from PIPELINE where CHANNEL_ID in
		<foreach open="(" close=")" collection="(_parameter instanceof java.util.Map ? (_parameter.containsKey(&quot;array&quot;) ? array : list) : _parameter)" item="item" separator=",">
			#{item}
		</foreach>
		ORDER BY ID DESC
	</select>
    <select id="listPipelines" resultMap="pipelineResult">
		select
		<include refid="allPipelineColumns" />
		from PIPELINE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			(NAME like concat('%',replace(#{searchKey},'_','\_'),'%')
			or ID like concat('%',replace(#{searchKey},'_','\_'),'%'))
			</if>
            <if test="channelId != null">AND CHANNEL_ID = #{channelId}</if>
        </trim>

		ORDER BY ID DESC

		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
    <select id="listPipelineByIds" resultMap="pipelineResult">
		select
		<include refid="allPipelineColumns" />
		from PIPELINE where ID in
		<foreach open="(" close=")" collection="(_parameter instanceof java.util.Map ? (_parameter.containsKey(&quot;array&quot;) ? array : list) : _parameter)" item="item" separator=",">
			#{item}
		</foreach>
		ORDER BY ID DESC
	</select>
    <update id="updatePipeline" parameterType="pipeline">
		update PIPELINE
		set
		NAME=#{name},
		PARAMETERS=#{parameters},
		DESCRIPTION=#{description},
		CHANNEL_ID=#{channelId},
		GMT_MODIFIED = now()
		WHERE ID = #{id}
	</update>
    <select id="checkPipelineUnique" parameterType="pipeline" resultType="Integer">
		select count(*) from PIPELINE
		where PIPELINE.ID != #{id}
		and PIPELINE.CHANNEL_ID = #{channelId}
		and PIPELINE.NAME = #{name}
	</select>
    <insert id="insertPipeline" parameterType="pipeline">
		insert into PIPELINE
		(NAME, PARAMETERS,DESCRIPTION,CHANNEL_ID,GMT_CREATE, GMT_MODIFIED)
		SELECT #{name},#{parameters},#{description},#{channelId},now(),now()
		from dual
		WHERE not exists (select * from PIPELINE
		where PIPELINE.CHANNEL_ID = #{channelId} and PIPELINE.NAME = #{name});
		<selectKey keyProperty="id" resultType="long" order="AFTER">
			select last_insert_id()
		</selectKey>
    </insert>
    <delete id="deletePipelineById" parameterType="long">
        delete from PIPELINE where ID = #{value}
    </delete>
    <select id="listByDestinationCondition" resultMap="pipelineResult">
		select
		<include refid="allPipelineColumns" />
		from PIPELINE
		<trim prefix="where" prefixOverrides="AND |OR ">
            <if test="searchKey != null and searchKey != ''">
			PARAMETERS like concat('%"destinationName":"',replace(#{searchKey},'_','\_'),'"%')
			</if>
        </trim>
		ORDER BY ID DESC
		<trim>
            <if test="offset != null">
                <if test="length != null">
					limit #{offset}, #{length}
				</if>
            </if>
        </trim>
    </select>
</mapper>
